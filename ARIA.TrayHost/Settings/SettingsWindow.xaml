<Window x:Class="ARIA.TrayHost.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ARIA Settings"
        Width="640" Height="480"
        MinWidth="560" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2e">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89b4fa"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="ARIA Settings" FontSize="22" FontWeight="SemiBold" Foreground="#89b4fa"/>
            <TextBlock Text="Configuration UI — coming in M11" FontSize="12" Foreground="#6c7086" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Placeholder content -->
        <Border Grid.Row="1"
                Background="#313244"
                CornerRadius="8"
                Padding="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="⚙" FontSize="48" HorizontalAlignment="Center" Foreground="#45475a"/>
                <TextBlock Text="Full settings UI is implemented in Milestone 11."
                           HorizontalAlignment="Center"
                           Foreground="#6c7086"
                           Margin="0,12,0,0"/>
                <TextBlock Text="Edit config.json directly for now."
                           HorizontalAlignment="Center"
                           Foreground="#6c7086"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Close" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
